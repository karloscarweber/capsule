<!DOCTYPE html>
<html lang="{{@site.locale}}"{{#match @custom.color_scheme "Dark"}} class="dark-mode"{{/match}}{{#match @custom.color_scheme "Auto"}} class="auto-color"{{/match}}>
<head>

    {{!-- Basic meta - advanced meta is output with {ghost_head} below --}}
    <title>{{meta_title}}</title>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="HandheldFriendly" content="True" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <style>
        :root {
            {{#if @custom.header_button_background}}
            --button-bg-color: {{@custom.header_button_background}};
            {{/if}}
            --button-text-color: {{#match @custom.header_button_text_color "Light"}}#fff{{else}}var(--color-darkgrey){{/match}};
        }
        section.announcement {
            background:#ff5030;
            background:#5E5CFF;
            display:flex;
            flex-direction: column;
            align-items: center;
            height:100%;
            min-height:95vh;
            width: 100%;
            min-width:95vw;
            position:absolute;
            left:0;
            top:0;
            z-index:1000;
            justify-content:center;
            overflow: hidden;
            padding: 1rem;
            border-radius:2rem;
        }

        :root {
          --animate-duration: 1s;
          --animate-delay: 1s;
          --animate-repeat: 1;
        }

        .animate__animated {
          -webkit-animation-duration: 1s;
          animation-duration: 1s;
          -webkit-animation-duration: var(--animate-duration);
          animation-duration: var(--animate-duration);
          -webkit-animation-fill-mode: both;
          animation-fill-mode: both;
        }

        @keyframes fadeInUp {
          from {
            opacity: 0;
            transform: translate3d(0, 50%, 0);
          }

          to {
            opacity: 1;
            transform: translate3d(0, 0, 0);
          }
        }

        .fadeInUp {
          animation-name: fadeInUp;
        }

        .hi-card {
            background:white;
            display:flex;
            flex-direction:column;
            align-items: center;
            width:100%;
            height:100%;
            max-width:26rem;
            max-height:36rem;
            box-sizing:border-box;
            padding:2rem;
            justify-content:space-between;
            border-radius:2rem;
            overflow-y: scroll;
        }

        /* Hide scrollbar for Chrome, Safari and Opera */
        .hi-card::-webkit-scrollbar {
          display: none;
        }

        /* Hide scrollbar for IE, Edge and Firefox */
        .hi-card {
          -ms-overflow-style: none;  /* IE and Edge */
          scrollbar-width: none;  /* Firefox */
        }

        .hi-button {
            display: block;
            position:relative;
            width: 100%;
            height: 3rem;
            line-height: 3rem;
            text-align:center;
            color:white;
            background: linear-gradient(90deg, #ff5030, #d65363, #ae5697, #8659cb, #5e5cff);
            text-decoration:none;
            border-radius: 24px;
            font-weight: 600;

            box-shadow: 0 0px 0px 0px rgba(94,92,255,0.0);
            border-radius: 30px;
            transition: all 0.12s ease-in-out;
        }
        .hi-button:hover {
            box-shadow: 0 7px 100px -25px rgba(94,92,255,0.46);
            transform: scale(1.05);
        }
        .text-box {
            text-align:justify;
            font-size:1.1rem;
            line-height:1.2em;
            font-weight:500;
            line-height: 1.3;
        }
        .greeting {
            padding:1rem;
            font-size:2rem;
        }

        @media screen and (max-width: 480px) {
            .page {
                top: 0 !important;
                left: 0 !important;
                bottom: 0 !important;
                right: 0 !important;
                border-radius: 0 !important;
            }
            section.announcement {
                border-radius: 0;
            }
            .hi-card {
                display: flex;
                left: 1rem;
                top: 1rem;
                bottom: 1rem;
                right: 1rem;
                position: absolute;
                max-width: calc(100% - 2rem);
                width: 100%;
                height: 100%;
                max-height: calc(100% - 2rem);
            }
            .greeting {
                padding:0rem;
                margin-left: 0.5rem;
            }
        }

    </style>

    {{!-- Theme assets - use the {asset} helper to reference styles & scripts,
    this will take care of caching and cache-busting automatically --}}
    <link rel="stylesheet" type="text/css" href="{{asset "built/screen.css"}}" />

    {{!-- This tag outputs all your advanced SEO meta, structured data, and other important settings,
    it should always be the last tag before the closing head tag --}}
    {{ghost_head}}

</head>
<body class="site-body">

<div class="page">
    <!--
    {{!-- special header section thing. --}}
    <section class="{{#match @custom.header_behaviour "sticky"}}sticky{{/match}}">
        <header class="gh-head{{#match @custom.publication_cover_style "!=" "None"}} has-cover{{/match}}">
            {{> "navbar"}}
        </header>
    </section>
    {{!-- /.section --}}
    -->
    <section class="announcement typeface-greycliffcf">

        <div class="hi-card animate__animated fadeInUp">
            <h3 class="greeting">👋<span style="display:inline-block;width:1rem;height:1rem;">&nbsp;</span>Hello Friends.</h3>
            <div class="text-box">
                <p style="text-align:center;">I'm Karl, my friends call me KOW.</p>
                <br>
                <p>I'm a web designer and developer living in Salt Lake City. My website is under construction while I work on something cool and new. Some FAX:</p>
                <ul style="padding:0;padding-left:1rem;">
                    <li>You can read my <a href="/blog/">blog</a>.</li>
                    <li>Read about the <a href="/blog/redesign/">active website redesign</a>.(started and stopped many times).</li>
                    <li>I'm working on modernizing <a href="https://github.com/camping/camping" target=_blank>Ruby Camping</a>, an old Ruby Web framework.</li>
                    <li>You can <a href="mailto:me@kow.fm?subject=freelance inquiry&body=We need your help designing and developing websites.">hire me</a> to design and develop websites. (new portfolio coming soon)</li>
                </ul>
            </div>

            <a class="hi-button" href="mailto:me@kow.fm?subject=Say Hi&body=Hi how are you.">Say Hi.</a>
        </div>

    </section>
    <!--
    <section class="stack">
        {{!-- All other templates get inserted here, index.hbs, post.hbs, etc --}}
        {{{body}}}
    </section>

    {{!-- The global footer at the very bottom of the screen --}}
    <section class="">
        <pagefooter>
                <section><a href="{{@site.url}}">{{@site.title}}</a> &copy; {{date format="YYYY"}}</section>
                <nav>
                    {{navigation type="secondary"}}
                </nav>
                <div><a href="https://ghost.org/" target="_blank" rel="noopener">Powered by Ghost</a></div>
        </pagefooter>
    </section>
-->
</div>
{{!-- /.page --}}

{{!-- Scripts - handle member signups, responsive videos, infinite scroll, floating headers, and galleries --}}
<script
    src="https://code.jquery.com/jquery-3.5.1.min.js"
    integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
    crossorigin="anonymous">
</script>
<script src="{{asset "built/capsule.js"}}"></script>
<script>
$(document).ready(function () {
    // Mobile Menu Trigger
    $('.gh-burger').click(function () {
        $('body').toggleClass('gh-head-open');
    });
    // FitVids - Makes video embeds responsive
    $(".gh-content").fitVids();
    $(".page").focus();
});
</script>

{{!-- Ghost outputs required functional scripts with this tag - it should always be the last thing before the closing body tag --}}
{{ghost_foot}}

</body>
</html>
